// Public configuration. Never put API keys or other secrets in this file.
// Enable only an owner-approved HTTPS endpoint implementing the README contract.
export const formConfig = Object.freeze({ endpoint: '' });
