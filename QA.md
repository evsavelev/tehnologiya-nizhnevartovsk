# QA и SEO — 7 сентября 2026

## Production V2 — проверено 10 сентября 2026

Публичный адрес: https://evsavelev.github.io/tehnologiya-nizhnevartovsk/.

Перед выпуском local/remote main совпали с `194ab554c927a1782ba3970de4057496508f085e`; ветка V2 — с `e281d4b4035175d0a138ff2d7750cd6932a6e097`. Рабочее дерево чистое, fast-forward возможен. Тег `v1-conversion-checkpoint` существует локально и в origin и указывает на базовый commit. Merge выполнен fast-forward, история сохранена, ветка не удалена.

Первый deployment: [34443751155](https://github.com/evsavelev/tehnologiya-nizhnevartovsk/actions/runs/34443751155), success, commit `e281d4b`. Публичный QA обнаружил зависимость скрытия мобильной панели от загрузки form-config. Исправление `508b84a5f2ad6799b345c84f66b66b11a521cb20`: обработчики фокуса устанавливаются до async import; HTML submit отключён до готовности формы, предотвращая нативный GET с полями. Регрессионный тест задерживает конфигурацию на 1200ms. Локально повторно пройдены V2 QA, 3 теста адаптера и SEO-check.

Исправленная реализация опубликована успешным [Actions run 34444075817](https://github.com/evsavelev/tehnologiya-nizhnevartovsk/actions/runs/34444075817). У job deploy и шага Deploy — success. После завершения проверялся **только публичный Pages URL**:

- 21 маршрут × 360/390/430/768/1440 = 105 сочетаний; HTTP 200, реальный HTTP 404 для неизвестного маршрута; 163 внутренние ссылки/якоря. Console/page errors и horizontal overflow отсутствуют.
- Меню, каталог, 12 работ портфолио и все фильтры, lightbox, стрелки/Escape/возврат фокуса, изображения, CTA. Обе полные цепочки от главной до заявки пройдены кликами, включая проект → связанную услугу.
- Form-config публично содержит `endpoint: ''`. Подготовка не вызывает POST или самопроизвольный mailto; статус «Обращение подготовлено, но ещё не отправлено». Доступны email, текст/копирование, MAX, телефон. Проверен отказ clipboard. Отдельные тесты успешного/ошибочного endpoint выполнялись исключительно с перехватом запросов в Playwright, production endpoint не изменялся, реальные заявки не отправлялись.
- Нижняя панель: реальные href телефона/MAX, переход к расчёту, targets ≥44px, скрытие при фокусе, меню и lightbox, отсутствие перекрытия footer. Проверены mobile viewports и имитация 20px safe-area в CSS; это не испытание физических устройств с вырезом экрана.
- До/после: точное совпадение исходного JPEG со сборкой, положение кадров и подписи, mouse drag, touch, Home/End/стрелки, aria-valuetext. Без JS — исходный официальный коллаж. Новые или неподтверждённые фото не добавлялись при выпуске.
- Reveal и живое переключение reduced-motion, отсутствие скрытых блоков без JS, skip-link, клавиатура. Hero и форма не анимируются. После исправления медленная загрузка настроек не мешает мобильному вводу.
- `node scripts/production-check.cjs`: 21 публичный HTML полностью совпадает с локальной сборкой, включая title, description, canonical, OpenGraph, Schema.org, alt и breadcrumbs. Публичные sitemap/robots, CSS/JS/config и JPEG сравнения также совпадают. Существующая структура SEO сохранена.

Production REDTEAM: первый экран понятен, реальные работы и каталог доступны, расчёт достижим с каждой страницы. Нет добавленного движения или редизайна, developer-настроек в интерфейсе, ложных заявлений об отправке, неподтверждённых обещаний. Сравнение использует спокойную подачу, источник и пояснение ракурсов. Открытых P0/P1/P2 после повторного QA нет.

Доказательства: `qa/report.json`, `qa/v2-report.json`, `qa/production-artifact.json`, снимки `qa/production-390-hero.png`, `qa/production-1440-hero.png`, `qa/production-comparison.png` и полный набор QA. Снимки hero теперь ожидают decode фотографии, чтобы не принимать промежуточный кадр загрузки за готовый экран. Последующий коммит отчётов/QA-инструмента не меняет публичный HTML/CSS/JS. Полевые CWV и реальная доставка в CRM не измерялись.

Для отката без переписывания истории: отменить отдельными revert исправление выпуска и V2-коммит, пересобрать и дождаться deployment; исходная точка — тег `v1-conversion-checkpoint`. Не применять reset/force-push к main.

## V2 — локальная проверка 10 сентября 2026

Текущая ветка: `v2-conversion-motion`; checkpoint `194ab554c927a1782ba3970de4057496508f085e`, тег `v1-conversion-checkpoint`. Нижележащие записи о публичной версии относятся к V1. V2 подготовлена к merge, не опубликована.

- Сборка: 21 статическая страница, все существующие URL сохранены.
- `node --test scripts/form-adapter.test.js`: 3 теста — запрет небезопасной конфигурации, контракт и строгий успех, таймаут/сетевая ошибка. Без обращений к настоящему получателю.
- `scripts/seo-check.cjs`: 21 уникальный title/description/canonical, sitemap, OpenGraph, статический контент, JSON-LD и breadcrumbs. Дополнительное сравнение с checkpoint: все 21 title/description/canonical/Schema.org, sitemap и robots совпадают; исходные alt внутренних страниц сохранены. На главной изменена выборка реальных работ, их исходные alt сохранены в портфолио.
- `npm test`: 21×5 = 105 сочетаний на 360/390/430/768/1440; 163 внутренние ссылки/якоря; HTTP 200, неизвестный маршрут 404; H1, alt, изображения, base paths, меню, фильтры, галерея, клавиатура и форма. Ошибок console/pageerror и горизонтального overflow нет.
- `scripts/v2-qa.cjs`: панель (touch targets ≥44px, скрытие при вводе, место над footer), отсутствие автоматического mailto, неподключённая форма, clipboard fallback, JSON payload и URL исходной страницы, pending/disabled/дубли, success, HTTP 500, HTML вместо JSON, success:false, таймаут, сохранение полей, стабильный requestId при повторе.
- Обе цепочки пройдены кликами: главная → каталог → услуга → кейс → заявка; главная → портфолио → кейс → связанная услуга → заявка. Тема передаётся в форму.
- Сравнение: мышь, touch, Home/End/стрелки и aria-valuetext; fallback-коллаж без JavaScript. Motion: появление один раз, живое переключение reduced-motion, весь контент видим без JS. Клавиатура: skip-link, галерея, возврат фокуса и форма.

### Визуальная проверка

Источник истины: reference lock V2 в DESIGN.md и исходный фирменный сайт. Просмотрены hero 360/390/430/768/1440, mobile contacts/form, desktop portfolio, категория печати и сравнение; остальные типовые страницы охвачены снимками полного QA. Сохранены светлый каталог, фирменные панели, настоящий логотип, естественные фото и размеры текста. Новый материал не заменяет оборудование или факты. Найденные проблемы и исправления — CLIENT-REDTEAM.md. Открытых P0/P1/P2 в проверенном объёме нет.

### Производительность относительно checkpoint

Chrome, холодные контексты, CPU 4×, 80ms latency, 200KB/s download, медиана трёх запусков на каждую ширину/версию. Локальный HTTP-сервер отдаёт несжатые ресурсы; это лабораторный сравнительный тест, не полевые CWV и не обещание скорости GitHub Pages.

| Метрика | V1 | V2 |
|---|---:|---:|
| LCP, 390px | 1032ms | 1192ms |
| LCP, 1440px | 1556ms | 1104ms |
| CLS, обе ширины | 0 | 0 |
| Передача первого экрана, 390px | 232075 B | 244456 B |
| Передача первого экрана, 1440px | 445517 B | 415352 B |
| Верх быстрых направлений, 390px | 915px | 907px |

Мобильный LCP вырос на 160ms: не называем это ускорением; абсолютный результат в этом тесте остаётся 1,19s. Различия загрузки lazy-фото и размеров видимого hero влияют на desktop-результат; не переносим цифры на реальных пользователей. Скачков layout нет. Оба JS-модуля формы исключены из запросов главной; config запрашивается на странице формы, adapter — при настоящей отправке.

CSS gzip: 5534 → 6664 B; основной JS: 1388 → 3742 B. Дополнительные модули config/adapter: 192/640 B gzip. Прирост CSS+основного JS — 3484 B gzip. Runtime-библиотеки не добавлены, hero сохраняет srcset/width/height/fetchpriority=high и не участвует в reveal. Lazy ниже первого экрана сохранён; сравнение загружает один общий JPEG 468730 B только в категории печати, не на главной. Видео: 0 B.

Доказательства: `qa/report.json`, `qa/v2-report.json`, `qa/v2-performance.json`, снимки `qa/v2-*` и `qa/390-*`, `qa/1440-*`; baseline `qa/v1/dist`. UTC в JSON может показывать 9 сентября — локальная дата проверки уже 10 сентября. Встроенный браузер не подключён; использован установленный Chrome через Playwright.

После merge и Actions требуется отдельный публичный QA. Фактическая доставка заявки требует разрешённого endpoint и проверки у конечного получателя; mock доказывает поведение интерфейса и контракт, но не доставку.

## Повторный аудит фирменного обновления

Объём: 21/21 маршрут, 105 сочетаний страницы и ширины 360/390/430/768/1440. Все 19 прежних адресов сохранены; добавлены `/catalog/events/photozones/` и `/catalog/fabrication/dimensional-objects/`. Базовое состояние до изменения — `43fbc3c`, checkpoint — `52f2a74`.

Повторный qiaomu-seo audit: mode `site inventory`, evidence `code + rendered`, аудит корпоративного каталога на русском языке. `scripts/seo-check.cjs` подтвердил 21 уникальный title, 21 уникальный description, 21 URL sitemap, точные canonical, Open Graph, один H1 по заданию, breadcrumbs и синтаксис Organization JSON-LD. Основной контент остаётся в статическом HTML. Авторская SVG-графика декоративна и не заменяет содержательные фотографии. Новые JPEG имеют размеры и alt. Правила robots/canonical и ограничения поисковых метрик — ниже, без изменений.

Реестр навыка проверен `validate_knowledge.py`: ошибок нет; просроченные записи об AI-поиске не использовались для утверждений. Внешние Google-источники robots/canonical повторно не менялись; проверены в этот же день (ссылки ниже). Никаких обещаний индексации и результатов продвижения.

Функциональный QA: меню, галереи, фильтры, форма, 404, MIME SVG/JPEG, GitHub Pages base paths; дополнен сквозным клиентским сценарием «Мероприятия → Фотозоны → Роснефть → расчёт» с автоподстановкой темы. Проверены три карточки с прямыми VK-ссылками на главной и `/news/`. Отдельный `scripts/verify-vk.cjs` подтвердил HTTP 200 и отображение официального автора/поста для всех трёх источников.

Визуальный REDTEAM и происхождение новых материалов: `CLIENT-REDTEAM.md`. Нижележащая история публикации относится к предыдущему релизу.

Текущая публикация подтверждена: Actions run [34117647542](https://github.com/evsavelev/tehnologiya-nizhnevartovsk/actions/runs/34117647542) успешен, реализация `1ba5d24a576dc91a2e0cc95310bb5a90cf3a1afe`. Полный QA публичного адреса https://evsavelev.github.io/tehnologiya-nizhnevartovsk/ повторно прошёл: 21 маршрут × 5 ширин, 74 ссылки/якоря, клиентский путь, новости, форма, меню, галерея, фильтры, ноль ошибок консоли. Страницы HTTP 200, неизвестный адрес HTTP 404; SVG/JPEG доступны с корректным MIME. Машинный результат сохранён в `qa/report.json`, проверка VK — в `qa/vk-links-report.json`. Индексация и эффект на обращения не измерялись. Следующие изменения документации не меняют опубликованный HTML/CSS/JS.

## История проверки первоначальной версии

## Объём

19/19 маршрутов, 95 сочетаний страницы и ширины: 360, 390, 430, 768, 1440 px. Инвентарь — `data/routes.json`; повторяемая проверка — `scripts/qa.cjs`. Браузер: Google Chrome через Playwright, Windows. Отчёт и скриншоты — локальная папка `qa/`.

## Функциональная проверка

- Все маршруты HTTP 200; неизвестный маршрут — настоящий HTTP 404.
- 70 внутренних ссылок/якорей: страницы доступны, цели якорей существуют.
- Нет горизонтального overflow на проверенных ширинах. Исправлено склеивание слов hero при скрытии переносов на мобильном.
- Мобильное меню: открытие, переход в каталог, закрытие Escape и фокус.
- Все пять фильтров портфолио; корректное количество работ 12/4/4/3/1.
- Галерея: открытие, кнопки, стрелки клавиатуры, Escape, возврат фокуса на исходную карточку.
- Форма: обязательные поля, автоподстановка услуги из URL, безопасное кодирование mailto. Письмо подготовлено; отправка на сервер не имитируется. Тестовое письмо не отправлялось.
- Ошибок JS и консоли не обнаружено.

## Визуальная проверка

Источник истины: существующий `DESIGN.md`, Freitag как основной промышленный каталог, T1 Energy только для доказательности фотографий. Сохранены белый фон, чёрные CTA, тонкие разделители, гротеск, модульная сетка и цвет только в реальных фотографиях/исходном логотипе.

Скриншоты главной, каталога, услуги, портфолио, производства, компании и контактов: 390 и 1440 px. Для полноразмерного захвата QA принудительно загружает все lazy images; в самом сайте отложенная загрузка сохранена. Изображения не подменяются пустыми блоками.

## SEO: site inventory, code + rendered

Цель: доступный поисковым системам корпоративный каталог для русскоязычной аудитории Нижневартовска, ХМАО и ЯНАО. Проверено всё дерево HTML, а не выборочная оценка главной.

| Проверка | Результат / доказательство |
|---|---|
| Уникальные title и description | 19/19, автоматическая проверка |
| Один H1 по заданию, H2/H3 | 19/19 H1; содержательные разделы шаблонов |
| Canonical | 19/19, точный публичный URL с префиксом репозитория |
| Open Graph | Заголовок, описание, URL, абсолютный адрес фотографии |
| HTML без зависимости от JS | Основной контент, ссылки и метаданные генерируются статически |
| Изображения | Alt, width/height, WebP, srcset, lazy ниже первого экрана |
| Favicon | Исходный логотип PNG |
| Structured data | JSON-LD Organization с подтверждёнными контактами, синтаксис проверен |
| Карта сайта | 19 URL, единые canonical и ссылки |
| Robots | Файл подготовлен; ограничение project hosting описано ниже |
| Хлебные крошки | Все вложенные маршруты, обычные HTML-ссылки |

Текущие правила сверены 07.09.2026: [Google — robots.txt](https://developers.google.com/search/docs/crawling-indexing/robots/intro), [Google — canonical](https://developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls).

Robots регулирует обход, canonical выражает предпочтение URL; ни одно не гарантирует индексацию. Для GitHub Pages project site robots в подпапке не управляет доменом. Файлы другого проекта в корне `evsavelev.github.io` не менялись; на собственном домене robots следует разместить в корне.

## Ограничения и следующие данные

Полевая производительность, поисковая индексация, позиции, трафик и Search Console не измерялись; результат QA не означает достижения этих результатов. Проверка JSON-LD не является подтверждением расширенного результата Google. Отсутствуют подтверждённый недельный график, точные модели/фото оборудования и согласованные технические параметры изделий. Эти сведения не выдуманы.

После согласования демо: получить данные оборудования и график от компании. После подключения собственного домена повторить URL/robots/canonical проверку; затем оценивать индексацию в Search Console через 7–14 дней при наличии доступа. Откат реализации возможен отдельным revert-коммитом, без сброса истории; checkpoint до продолжения — `c523a5d`.

## Публикация — проверено

GitHub Actions [34114519871](https://github.com/evsavelev/tehnologiya-nizhnevartovsk/actions/runs/34114519871) завершился успешно. Реализация `7f3fd3cbf088b3292d352cb6f1af3888bb1a6fb7` опубликована на https://evsavelev.github.io/tehnologiya-nizhnevartovsk/ с HTTPS.

Повторный полный QA публичной версии 07.09.2026 прошёл: 19 страниц × 5 ширин, 70 ссылок/якорей, все фильтры, мобильное меню, галерея и форма, ноль ошибок консоли. Главная и все страницы HTTP 200, несуществующая страница HTTP 404. Публичные CSS, JS, фотографии, sitemap, robots и favicon доступны. Скриншоты публичной версии сохранены в `qa/`.

Визуальная проверка выполнена против DESIGN.md; открытых P0/P1/P2 не обнаружено. На снимках сайта проверены фактические фотографии здания, арки, вывесок и оформление. Локальная проверка и проверка после публикации согласуются. Последующие изменения этого отчёта не меняют HTML/CSS/JS сайта.

Стадии SEO: реализовано — да; опубликовано и наблюдаемо — да; обработано поисковой платформой — не проверено; эффект на трафик/обращения — не измерен. Корневой https://evsavelev.github.io/robots.txt отвечает 404; ограничение размещения robots описано выше, блокирующих директив там не обнаружено.
