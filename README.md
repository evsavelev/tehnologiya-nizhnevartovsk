# Технология — корпоративный демо-каталог

## V2: опубликована и проверена

V2 выпущена 10.09.2026: fast-forward `e281d4b` и исправление раннего мобильного ввода `508b84a`. Публичный QA пройден на 21 странице и пяти ширинах; Actions run `34444075817` успешен. Ветка `v2-conversion-motion` и тег отката `v1-conversion-checkpoint` сохранены; исходный commit `194ab554c927a1782ba3970de4057496508f085e`. Подробности выпуска: QA.md; изменения и запрос материалов: [V2-IMPROVEMENTS.md](V2-IMPROVEMENTS.md).

### Подключение настоящей отправки

Одна настройка: `endpoint` в `data/form-config.js`. Сейчас пустая строка: форма подготавливает обращение и предлагает email / MAX / звонок; никакого сообщения об отправке и автоматического открытия почтовой программы нет. Текст можно скопировать; при запрете clipboard он доступен для ручного выделения. Данные не сохраняются в localStorage и не уходят на сторонний сервис.

Для включения владелец должен предоставить **подтверждённый HTTPS endpoint** и получателя заявок (CRM, ящик или ответственное лицо), согласовать правила обработки данных и текст информирования посетителя. В `endpoint` указывается полный URL, после чего выполняется сборка. Секреты и API-ключи нельзя помещать в репозиторий или клиентский JavaScript; авторизация к CRM выполняется на сервере владельца. Случайный внешний сервис не подключён.

Контракт адаптера (`src/form-adapter.js`): POST JSON с `name`, `contact`, `service`, `comment`, `sourceUrl`, `requestId`. `sourceUrl` — URL страницы, CTA которой привёл к форме, без query/hash; при прямом посещении — адрес контактов. `requestId` остаётся тем же при повторе одинакового обращения в текущей вкладке. Сервер обязан валидировать поля, ограничивать частоту запросов, дедуплицировать по `requestId` и возвращать **HTTP 2xx + JSON `{"success":true}` только после надёжного принятия заявки**. HTML, 2xx без подтверждения, отрицательный ответ, сеть и таймаут не считаются успехом. Повторная автоматическая отправка не выполняется; таймаут 12 секунд, введённые данные сохраняются в форме. Идемпотентность на клиенте не заменяет серверную.

Endpoint должен разрешать CORS для `https://evsavelev.github.io`: POST/OPTIONS, заголовки Content-Type/Accept, без cookie. Для локальной проверки — отдельно согласованный localhost origin. Адаптер не использует credentials и запрещает redirects. GitHub Pages принимает только статическую сборку, сервер сюда не добавляется.

Перед включением: проверить разрешённое тестовое обращение до конечного получателя, ответы ошибки/таймаута и защиту от дублей. Текущие тесты используют локальный mock, реальных заявок не отправляют.

### Проверки V2

```sh
npm run build
node --test scripts/form-adapter.test.js
node scripts/seo-check.cjs
# При запущенном npm start:
npm test
node scripts/v2-qa.cjs
node scripts/v2-performance.cjs
```

Performance-скрипт требует сохранённую копию V1 в `qa/v1/dist` и свободный порт 4187. Для воспроизведения на другой машине соберите checkpoint в отдельном checkout и скопируйте его `dist` туда; не переключайте и не перезаписывайте рабочую ветку. Он сравнивает 3 холодных запуска при 4× CPU / 80ms / 200KB/s; результат лабораторный. Не запускайте одновременно с другими браузерными тестами. QA использует Chrome через Playwright; скриншоты и JSON сохраняются локально в `qa/`.

Для будущих изменений перед merge выполните сборку и проверки; push main автоматически запускает Actions. Для публичного QA задайте `QA_URL=https://evsavelev.github.io/tehnologiya-nizhnevartovsk/` и запустите UX-наборы; `node scripts/production-check.cjs` дополнительно сравнивает весь опубликованный HTML/SEO и ключевые ресурсы с текущим dist. Откат выпуска — отдельные revert коммитов исправления и V2, повторная сборка и deployment, без удаления исходных материалов или переписывания истории.

Демо рекламно-производственной компании из Нижневартовска. 21 статическая страница: главная, каталог, шесть категорий, пять услуг, портфолио с 12 работами, три кейса, производство, о компании, новости из официального VK, контакты. Все прежние 19 адресов сохранены.

Фирменное обновление: графитово-бронзовые панели с самостоятельным SVG-мотивом по визуальному языку VK-обложки. Каталог и текстовые разделы остаются светлыми. Клиентский сценарий и выводы проверки — `CLIENT-REDTEAM.md`.

Публичный адрес: https://evsavelev.github.io/tehnologiya-nizhnevartovsk/

## Локальная работа

Node.js 24. В PowerShell используйте `npm.cmd`, если запуск npm.ps1 запрещён политикой Windows.

```sh
npm ci
npm run build
npm start
# В другом терминале:
npm test
node scripts/seo-check.cjs
# Разовая проверка доступности исходных постов VK (требует интернета):
node scripts/verify-vk.cjs
```

Открыть http://127.0.0.1:4173/tehnologiya-nizhnevartovsk/. QA использует установленный Google Chrome. Скриншоты и машинный отчёт сохраняются в `qa/` (не публикуются). Для проверки опубликованной версии задайте `QA_URL` с завершающим `/`.

## Архитектура и контент

- `data/catalog.js` — категории, варианты, услуги и связи с проектами.
- `data/projects.js`, `data/news.js` — портфолио и фотообзоры.
- `data/company.js` — контакты; `data/media.json` — происхождение фотографий.
- `data/catalog-guidance.js` — исходные данные для расчёта в каждом направлении.
- `src/components.js` — переиспользуемые компоненты и URL конфигурация.
- `scripts/build.js` — сборка HTML, метаданных, sitemap и 404 в `dist/`.
- `src/styles.css`, `src/app.js` — адаптивная сетка, меню, галерея, фильтры, сравнение, motion и состояния формы; `src/form-adapter.js` — транспорт отправки.
- `DESIGN.md` — утверждённое направление и дизайн-ограничения.
- `QA.md` — результат проверки и ограничения.

Чтобы добавить услугу, внесите объект в `services`, укажите её ID в категории и связанные проекты, затем пересоберите. Новые категории автоматически получают страницу. Подробный кейс создаётся для проекта с `page: true`. Для смены домена измените `base` и `origin` в компонентах, пересоберите и проверьте ссылки/canonical/sitemap.

Связи категории с услугами вычисляются из поля `category` каждой услуги. Для предметной перелинковки задайте `relatedServices` в таблице `related` в `data/catalog.js`. Формат сохраняет три уровня каталога; при расширении добавляются данные и связи, а не копии HTML-шаблонов. Для сотен позиций следующим этапом можно добавить подкатегории и постраничный вывод, после согласования структуры с компанией.

## Ручное обновление новостей

Новости редактируются в `data/news.js` и собираются на главной и `/news/`. Для записи нужны уникальный `id`, заголовок, короткий пересказ, прямая `vk`-ссылка вида `https://vk.ru/wall-179232832_...`, реальный `imageFile` с размерами либо номер существующей фотографии `image`, содержательный `alt`, тематическая `category` и при наличии `project`. `dateLabel` заполняется только датой, видимой в источнике; относительные даты не пересчитываются. `sourceNote` и `verified` служат для редакторской проверки и не выводятся посетителю.

Проверить публикацию и принадлежность фотографии, сохранить изображение локально и записать происхождение в `data/media.json`, затем собрать, проверить и отправить изменения. Никакой API-интеграции, автоматического импорта, фоновой синхронизации или CMS нет. `scripts/verify-vk.cjs` только проверяет доступность уже выбранных ссылок и не изменяет контент.

До настройки endpoint форма не отправляет данные на сервер: пользователь выбирает резервный канал и подтверждает отправку в нём. Никаких аналитических счётчиков, регистрации, загрузки файлов, корзины или оплаты нет.

## Публикация

GitHub Pages из GitHub Actions. Каждый push в `main` запускает `.github/workflows/pages.yml`, собирает только публичный каталог `dist` и публикует его. Исходные материалы клиента, локальные инструменты, credentials и QA-скриншоты не входят в артефакт сайта.

`robots.txt` подготовлен в папке проекта. На GitHub Pages project site правила обхода определяются корневым `/robots.txt` домена; файл внутри `/tehnologiya-nizhnevartovsk/` не заменяет его. При переносе на собственный домен разместить файл в корне. Sitemap и canonical уже используют публичные адреса страниц.

## Материалы и данные для полной версии

Реальные фотографии и оригинальный PNG-логотип из https://disk.yandex.ru/d/BbMGtYuzDdbgSA. Оригиналы не изменены; WebP — отдельные оптимизированные копии. Контакты сверены 07.09.2026 по официальному VK. Проверка карточки 2ГИС подтверждает адрес и ссылки; интерактивный доступ ограничен CAPTCHA. Не выводим рейтинг, неподтверждённый недельный график, старый телефон из задания, модели станков, сроки, цены или численные обещания.

Для полной версии нужны подтверждённые характеристики и фотографии оборудования, недельный график, технические параметры изделий и согласованные описания задач/результатов кейсов. Фото здания уже использовано на странице компании. Сейчас технологии представлены без вымышленных моделей станков.

## Расширение

2. CMS / административная панель: заменить источник данных, сохранив поля и URL.
3. Калькуляторы: согласовать параметры и правила расчёта с компанией.
4. Корзина / магазин: добавить только к стандартизированным товарам.
5. Оплата: после согласования магазина и договорных условий.
6. Личный кабинет / CRM: отдельный серверный слой и реальные статусы заявок.

Эти этапы не реализованы и не показываются в интерфейсе как пустые функции.
